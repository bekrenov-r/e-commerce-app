docker compose build
docker compose up
* If catalog haven't started - restart it.
    - docker compose up catalog
* Delete dbs:
    - docker compose down --volumes
* Delete microservice images (before docker compose up):
    - docker image prune -a --filter "label=com.docker.compose.project=e-commerce"